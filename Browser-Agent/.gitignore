# Hugging Face secret token — never commit
hf-token

# Neon Postgres DB credentials (DATABASE_URL) — never commit
neon-db

# Env files with real secrets — never commit (only .env.example is committed)
.env
*.env
!.env.example
profilesync/.env

# Gateway API key — never commit
api-key

# Pasted screenshots / scratch images
image.png
